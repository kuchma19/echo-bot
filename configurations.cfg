bot_type {
    type = "telegram"
}

bot_messages {
    help = "I'm echo-bot. I will repeat your messages. You can write command /repeat to change the number of repetition."
    repeat = "Current repetition count is {count}."
    repetition_count = 1
}

log {
    directory = "."
    file_name = "log"
    min_level = "debug"
}
